import { CartesianGrid, Line, LineChart, ResponsiveContainer, Tooltip, XAxis, YAxis } from 'recharts';
import DashboardPanel from './DashboardPanel.jsx';
import { finiteNumber } from '../../utils/performanceCharts.js';
export default function PerformanceLineChart({title,subtitle,rows,metric,onSelect}){const data=rows.map((row)=>({...row,value:finiteNumber(row[metric])}));return <DashboardPanel title={title} subtitle={subtitle}>{!data.length?<div className="flex h-56 items-center justify-center text-sm text-muted">No data to visualize.</div>:<ResponsiveContainer width="100%" height={250}><LineChart data={data} margin={{top:12,right:12,left:0,bottom:14}} onClick={(state)=>state?.activePayload?.[0]&&onSelect?.(state.activePayload[0].payload)}><CartesianGrid stroke="#242c38" vertical={false}/><XAxis dataKey="name" stroke="#667080" tick={{fontSize:10}}/><YAxis domain={metric==='winRate'?[0,100]:['auto','auto']} stroke="#667080" tick={{fontSize:10}}/><Tooltip contentStyle={{background:'#0d1118',border:'1px solid #242c38',borderRadius:12}}/><Line type="monotone" dataKey="value" stroke="#c7f36b" strokeWidth={2} dot={{r:4,fill:'#c7f36b'}} activeDot={{r:6}}/></LineChart></ResponsiveContainer>}</DashboardPanel>}
