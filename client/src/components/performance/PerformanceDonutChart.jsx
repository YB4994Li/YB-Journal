import { Cell, Legend, Pie, PieChart, ResponsiveContainer, Tooltip } from 'recharts';
import DashboardPanel from './DashboardPanel.jsx';
const colors=['#c7f36b','#60a5fa','#94a3b8','#fbbf24','#a78bfa','#2dd4bf'];
export default function PerformanceDonutChart({title,rows,onSelect}){const data=rows.filter((r)=>r.totalTrades>0).map((r)=>({name:r.name,value:r.totalTrades,row:r}));return <DashboardPanel title={title} subtitle="Share of total trades">{!data.length?<div className="flex h-56 items-center justify-center text-sm text-muted">No distribution data.</div>:<ResponsiveContainer width="100%" height={250}><PieChart><Pie data={data} dataKey="value" nameKey="name" innerRadius={55} outerRadius={88} paddingAngle={2} onClick={(entry)=>onSelect?.(entry.row||entry.payload?.row)}>{data.map((item,index)=><Cell key={item.name} fill={colors[index%colors.length]}/>)}</Pie><Tooltip contentStyle={{background:'#0d1118',border:'1px solid #242c38',borderRadius:12}}/><Legend wrapperStyle={{fontSize:11}}/></PieChart></ResponsiveContainer>}</DashboardPanel>}
