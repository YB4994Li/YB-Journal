import { Cell,Legend,Pie,PieChart,ResponsiveContainer,Tooltip } from 'recharts';
import DashboardPanel from './DashboardPanel.jsx';
import { seriesColor } from './chartPalette.js';
const tooltipStyle={background:'#0d1118',border:'1px solid #303947',borderRadius:12,boxShadow:'0 14px 34px rgba(0,0,0,.42)',fontSize:12};
export default function PerformanceDonutChart({title,rows,onSelect}){const data=rows.filter((r)=>r.totalTrades>0).map((r)=>({name:r.name,value:r.totalTrades,row:r})),total=data.reduce((sum,item)=>sum+item.value,0);return <DashboardPanel title={title} subtitle="Share of total trades">{!data.length?<div className="flex h-56 items-center justify-center text-sm text-muted">No distribution data.</div>:<ResponsiveContainer width="100%" height={250}><PieChart><Pie data={data} dataKey="value" nameKey="name" innerRadius={58} outerRadius={90} paddingAngle={2} cornerRadius={4} stroke="#111722" strokeWidth={2} isAnimationActive animationDuration={500} animationEasing="ease-out" onClick={(entry)=>onSelect?.(entry.row||entry.payload?.row)}>{data.map((item,index)=>{const color=seriesColor(index);return <Cell key={item.name} fill={color} style={{filter:`drop-shadow(0 0 4px ${color}4d)`}}/>})}</Pie><text x="50%" y="47%" textAnchor="middle" fill="#f1f5f9" fontSize="20" fontWeight="600">{total}</text><text x="50%" y="57%" textAnchor="middle" fill="#94a3b8" fontSize="10">TRADES</text><Tooltip contentStyle={tooltipStyle}/><Legend wrapperStyle={{fontSize:11,color:'#a8b2c1'}}/></PieChart></ResponsiveContainer>}</DashboardPanel>}
