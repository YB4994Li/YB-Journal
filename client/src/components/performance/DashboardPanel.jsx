export default function DashboardPanel({title,subtitle,children,className=''}){if(!title&&!subtitle)return children;return <section className={`card min-w-0 p-4 shadow-[0_14px_38px_rgba(0,0,0,.18)] ${className}`}><div className="mb-3"><h3 className="text-sm font-semibold tracking-tight">{title}</h3>{subtitle&&<p className="mt-0.5 text-xs leading-relaxed text-muted">{subtitle}</p>}</div>{children}</section>}
