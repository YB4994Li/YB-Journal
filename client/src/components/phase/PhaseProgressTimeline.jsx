import PhaseStatusBadge from './PhaseStatusBadge.jsx';
export default function PhaseProgressTimeline({phases}){return <div className="flex flex-col gap-2 sm:flex-row sm:items-center">{phases.map((phase,index)=><div key={phase.id} className="flex flex-1 items-center gap-2"><div className="flex flex-1 items-center justify-between rounded-xl border border-line bg-ink/40 p-3"><div><p className="text-xs text-muted">Step {index+1}</p><p className="mt-1 text-sm font-semibold">{phase.name}</p></div><PhaseStatusBadge status={phase.status}/></div>{index<phases.length-1&&<span className="hidden text-muted sm:block">→</span>}</div>)}</div>;}
