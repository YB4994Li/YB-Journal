import {money,number} from '../../utils/format.js';
import PhaseStatusBadge from './PhaseStatusBadge.jsx';
export default function PhaseOverview({phase,currency,onSettings}){if(!phase)return null;return <section className="card flex flex-wrap items-center gap-5 p-4"><div><p className="text-lg font-semibold">{phase.name}</p><div className="mt-1"><PhaseStatusBadge status={phase.status}/></div></div><div><p className="label">Initial balance</p><p>{money(phase.initialBalance,currency)}</p></div><div><p className="label">Profit target</p><p>{phase.profitTargetPercentage==null?'—':`${number(phase.profitTargetPercentage)}%`}</p></div><div><p className="label">Max / daily loss</p><p>{phase.maximumLossPercentage??'—'}% / {phase.dailyLossLimitPercentage??'—'}%</p></div><div><p className="label">Minimum days</p><p>{phase.minimumTradingDays??'—'}</p></div><button className="btn-secondary ml-auto" onClick={onSettings}>Phase settings</button></section>;}
